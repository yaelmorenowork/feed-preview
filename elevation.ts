/**
 * Shared soft "premium card" shadow used across the widget's
 * top-level surfaces (Feed Preview, Onboarding, error fallback).
 * Centralized so every surface stays visually consistent and the
 * value only needs to change in one place.
 */
export const CARD_SHADOW =
  "0 1px 2px rgba(15, 15, 15, 0.04), 0 8px 24px -8px rgba(15, 15, 15, 0.08), 0 16px 40px -16px rgba(15, 15, 15, 0.06)";
